name: AURA CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./src

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: './src/package-lock.json'

      - name: Install dependencies
        run: npm ci

      - name: Typecheck
        run: set -o pipefail; npm run typecheck 2>&1 | tee typecheck.log

      - name: Upload typecheck diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: diagnosis-v2-5-c-typecheck
          path: ./src/typecheck.log
          if-no-files-found: error

      - name: Diagnosis V2.5-C focused tests
        run: npm test -- diagnosisEvidenceIdentityV1.test.ts diagnosisV2_5Projection.test.ts formalDiagnosisV2_5.test.ts

      - name: Diagnosis regression tests
        run: npm test -- diagnosis

      - name: Build
        run: npm run build
